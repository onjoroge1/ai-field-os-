name: Field OS

on:
  pull_request:
    paths:
      - "erpnext/field_os/**"
      - ".github/workflows/field-os.yml"
  push:
    branches: [develop]
    paths:
      - "erpnext/field_os/**"

permissions:
  contents: read

jobs:
  field-os-static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - name: Compile Field OS Python
        run: python -m compileall -q erpnext/field_os
      - name: Run isolated Field OS regression tests
        run: python erpnext/field_os/tests/run_unit.py

  equipment-live:
    name: Equipment acceptance (MariaDB)
    runs-on: ubuntu-latest
    timeout-minutes: 35
    services:
      mariadb:
        image: mariadb:11.8
        env:
          MARIADB_ROOT_PASSWORD: fieldos-test-root
        ports: ["3306:3306"]
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=10s --health-timeout=5s --health-retries=10
      redis:
        image: redis:7.2
        ports: ["6379:6379"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - name: Install runtime prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y libmariadb-dev mariadb-client pkg-config
          pip install frappe-bench
          npm install -g yarn@1.22.22
      - name: Create disposable Frappe and ERPNext site
        run: |
          bench init "$RUNNER_TEMP/fieldos-bench" --frappe-branch develop --skip-assets --skip-redis-config-generation --python python3.14
          cd "$RUNNER_TEMP/fieldos-bench"
          bench get-app --skip-assets erpnext "$GITHUB_WORKSPACE"
          bench set-config -g redis_cache redis://127.0.0.1:6379/0
          bench set-config -g redis_queue redis://127.0.0.1:6379/1
          bench set-config -g redis_socketio redis://127.0.0.1:6379/2
          bench new-site fieldos.test --db-type mariadb --db-host 127.0.0.1 --mariadb-user-host-login-scope '%' --db-root-password fieldos-test-root --admin-password fieldos-test-admin
          bench --site fieldos.test install-app erpnext
          bench --site fieldos.test set-config allow_tests true
      - name: Verify real equipment persistence and tenant permissions
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_equipment_live.run
      - name: Build operator assets and install browser
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench build --apps frappe,erpnext
          npm install -g agent-browser@0.38.1
          agent-browser install
      - name: Verify real estimates, parts and customer approvals
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_estimates_live.run
      - name: Verify manager and technician browser workflows
        env:
          FIELD_OS_TEST_PASSWORD: fieldos-browser-acceptance
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_equipment_live.seed_browser
          env/bin/python apps/erpnext/erpnext/field_os/tests/integration/test_equipment_browser.py
      - name: Verify real recurring agreements, dispatch and renewals
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_agreements_live.run
      - name: Verify estimate delivery and customer approval in browser
        env:
          FIELD_OS_TEST_PASSWORD: fieldos-browser-acceptance
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_estimates_live.prepare_estimates
          env/bin/python apps/erpnext/erpnext/field_os/tests/integration/test_estimates_browser.py
      - name: Verify completion evidence, stock, accounting and invoice delivery
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_completions_live.run
      - name: Verify maintenance agreements in browser
        env:
          FIELD_OS_TEST_PASSWORD: fieldos-browser-acceptance
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_agreements_live.prepare_agreements
          env/bin/python apps/erpnext/erpnext/field_os/tests/integration/test_agreements_browser.py
      - name: Verify technician completion and billing in browser
        env:
          FIELD_OS_TEST_PASSWORD: fieldos-browser-acceptance
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          env/bin/python apps/erpnext/erpnext/field_os/tests/integration/test_completions_browser.py
      - name: Verify native company onboarding
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_onboarding_live.run
      - name: Verify owner onboarding and new technician login in browser
        env:
          FIELD_OS_TEST_PASSWORD: fieldos-browser-acceptance
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_onboarding_live.prepare_onboarding
          env/bin/python apps/erpnext/erpnext/field_os/tests/integration/test_onboarding_browser.py
      - name: Verify native CSV migration and rollback
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_migrations_live.run
      - name: Verify owner migration in browser
        env:
          FIELD_OS_TEST_PASSWORD: fieldos-browser-acceptance
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_migrations_live.prepare_migrations
          env/bin/python apps/erpnext/erpnext/field_os/tests/integration/test_migrations_browser.py
      - name: Verify native demo seed, isolation and financial preservation on reset
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_demo_live.run
      - name: Verify guided demo and reset in browser
        env:
          FIELD_OS_TEST_PASSWORD: fieldos-browser-acceptance
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_demo_live.prepare_demo
          env/bin/python apps/erpnext/erpnext/field_os/tests/integration/test_demo_browser.py
      - name: Verify commercial quotas and owner plan screen
        env:
          FIELD_OS_TEST_PASSWORD: fieldos-browser-acceptance
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_commercial_live.run
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_onboarding_live.prepare_onboarding
          env/bin/python apps/erpnext/erpnext/field_os/tests/integration/test_commercial_browser.py
      - name: Verify subscription billing persistence and signed events
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_billing_live.run
      - name: Verify scoped support grants and immutable audit
        run: |
          cd "$RUNNER_TEMP/fieldos-bench"
          bench --site fieldos.test execute erpnext.field_os.tests.integration.test_support_live.run
      - name: Preserve commercial browser screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: commercial-browser
          path: ${{ runner.temp }}/fieldos-bench/logs/commercial-*.png
      - name: Preserve demo browser screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: demo-browser
          path: ${{ runner.temp }}/fieldos-bench/logs/demo-*.png
      - name: Preserve migration browser screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: migrations-browser
          path: ${{ runner.temp }}/fieldos-bench/logs/migrations-*.png
      - name: Preserve onboarding browser screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: onboarding-browser
          path: ${{ runner.temp }}/fieldos-bench/logs/onboarding-*.png
      - name: Preserve completion browser screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: completions-browser
          path: ${{ runner.temp }}/fieldos-bench/logs/completions-*.png
      - name: Preserve agreement browser screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agreements-browser
          path: ${{ runner.temp }}/fieldos-bench/logs/agreements-*.png
      - name: Preserve estimate browser screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: estimates-browser
          path: ${{ runner.temp }}/fieldos-bench/logs/estimates-*.png
      - name: Preserve browser screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: equipment-browser
          path: ${{ runner.temp }}/fieldos-bench/logs/equipment-*.png
      - name: Preserve failure logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: equipment-site-logs
          path: ${{ runner.temp }}/fieldos-bench/logs/
